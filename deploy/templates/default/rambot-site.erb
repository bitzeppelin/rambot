server {
  listen   80;
  server_name  <%= node['rambot']['server_name'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/rambot.access.log;
  error_log   <%= node['nginx']['log_dir'] %>/rambot.error.log;

  charset utf-8;

  location / {
    proxy_pass http://127.0.0.1:<%= node['rambot']['port'] %>;
  }
}
